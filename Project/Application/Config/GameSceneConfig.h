#pragma once
namespace Application
{
	inline constexpr int32_t kBlockContainerRows = 8; // ブロックコンテナの行数
	inline constexpr int32_t kBlockContainerCols = 6;  // ブロックコンテナの列数

	inline constexpr float kBlockSize = 64.0f; // ブロックの1辺の長さ

	inline constexpr float kInitialTemperature = 12.0f; // 温度計の初期温度
}